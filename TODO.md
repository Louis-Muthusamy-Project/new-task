# TODO

## Websites Cloudinary + Mongo persistence
- [x] Update template flow so TemplateLibraryModal provides `templateZipCloudinaryUrl` and `WebsitesTab.jsx` passes it to `/website/setup`.
- [ ] Wire favicon file upload in `frontend/src/pages/WebsiteBuilder/websiteWizard/WebsiteSetupPage.jsx` to Cloudinary and persist `faviconUrl` in MongoDB on Save.

- [ ] Run frontend build/tests (or lint) to ensure no runtime/type errors.


